import React from 'react';
import FanetLayout from '@/components/FanetLayout';

export default function DashboardLayout({ children }: { children: React.ReactNode }) {
    return <FanetLayout>{children}</FanetLayout>;
}
